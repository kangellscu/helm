apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lnmp-with-repo.fullname" . }}-test-connection"
  labels:
{{ include "lnmp-with-repo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "lnmp-with-repo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
